# 05 — Desk review: `2ace92f1` "the desk now knows what is on today, and says what it is missing"

Reviewer: systems-reviewer, REVIEW mode. Scope: `git diff 927f192f..2ace92f1 -- src/lib/chat` (16 files, +1840/−56). Worktree `sports-betting-trends-redesign`, branch `site-redesign`; `git status --porcelain -- src/lib/chat` is empty, so every `file:line` below is the committed text. Nothing under `src/app` was read for findings. Nothing was modified except this file; nothing external was called; 2 of the 4 permitted probes were spent (both 200, before the cookieless cap).

Executed: `npx vitest run src/lib/chat` → 14 files / 360 tests green (1.96 s). `npx tsc --noEmit` → 0 errors under `src/lib/chat`. Three read-only scratch scripts against the real modules (session scratchpad: `g1-grounding-smuggle.ts`, `g2-slate-missing.ts`, `g3-router-fp.ts`) plus the dev server's own `[chat/turn]` log (`scratchpad/next-dev-3101.log`, 17 Lane B turns) and two live probes (`review-probe-1.json`, `review-probe-2.json`).

Threat brief: `03-threat.md` covers the site IA change, not this desk change (only S6/AC14 touch chat, and both still hold — scope pin at `router.ts:585`). So the verdict is computed from my own threat pass.

---

## Verdict: ANOTHER ROUND

Two `must` findings, both proven by execution (no refuter needed); the rest are shoulds and nits. The change fixes what it says it fixes — the measured "94 hours old" rejections, the October-NBA misroute, the matchup-shaped fallback — and the tests are real. But the mechanism chosen for the grounding fix widens the fabrication surface on the guard that is the product's brand, and the schedule tool turns a data outage into a confident "nothing is on". Both fixes are small.

## The kill test

Friday night the odds refresh fails silently (the MLB *model* file already sat 94 h stale this week, per the builder's own log). Saturday 7 PM a reader asks "what games are on tonight?". `buildSlateEntities` finds zero games dated today (every row in the stale file is Friday's), the router still tags the turn `schedule` (`router.ts:666-674` runs even when every league is dark, by design), and `get_todays_slate` hands the model `"Nothing on the board anywhere today (Saturday, September 12, 2026 ET). Dark: MLB, NFL, NBA, WNBA."` with no warning anywhere in the payload — `slate.ts:199-200` reads `fetchedAt` and `events` and drops `dataWarning` on the floor. The prompt tells the model the note is safe to read aloud verbatim (`laneB.ts:54`), so the desk tells a reader there is no baseball on a 15-game Saturday. The only log line about the stale file fired once at cold start (`tools/index.ts:62,111`) and nothing in `[chat/turn]` distinguishes "dark" from "unreadable". Nobody notices until someone compares the site to ESPN.

## Findings

Refuted at the gate: 0 — every blocker/must below was proven by execution, so none was sent to a blind refuter. One candidate was self-refuted at the finding stage and dropped (a receipts-lane regression from the `confidence` rescale: `src/lib/chat/nfl/tools.ts` emits only `calibratedConfidence`, never `confidence`).

**1. [must] — `grounding.ts:427-434` + `:539-541` → numbers inside `rule`, `reasoning`, `notes` (and `dataWarning`, `:423`) now ground ANY claim in the reply, for any game, in any bucket.**
Mechanism: `harvestText` (`:539`) runs `extractClaims` over every TEXT_VALUE_KEYS string and pushes every value into `quotedValues` (`:541`) — and every `N%` into `quotedPercents` as well. `groundsAsQuoted` (`:873`) is then consulted for percent claims (`:704`), sub-1 decimals (`:716`) and prices/stats (`:738`). The bucket is context-free: a number is "real" if it appears anywhere in any payload of the turn. Before the commit, `get_dream_memory`'s `rule`/`reasoning` contributed nothing; now they contribute everything. Executed (`g1`, against the real module, haystack = tonight's real Braves odds + ONLY the four *committed* memory seeds at `memory.ts:311-343`; production adds every nightly rule on top):

| reply | odds only (before) | + get_dream_memory (after) |
|---|---|---|
| "Braves are a **20% edge** tonight, hammer it." | REJECTED `["20%"]` | **GROUNDED** |
| "The model has the Braves at **90%** tonight." | REJECTED `["90%"]` | **GROUNDED** |
| "Clean **15.1% edge** on the Phillies here." | — | **GROUNDED** |
| "That is an **18% edge**, bet it big." | — | **GROUNDED** |
| "Only a **2.4% edge**, pass." | — | **GROUNDED** |
| "Take the Braves at -190." (control) | REJECTED | REJECTED `["-190"]` |

The four seeds alone contribute 24 numeric tokens, 9 of them percents (3, 3, 18, 90, 14.6, 15.1, 2.4, 20, 20); the Unicode minus in "−18% to −90% EV" is not a sign to the extractor, so they land as positive percents. Frequency: `get_dream_memory` is in `toolsUsed` on **11 of the 17** Lane B turns in the dev log, so this corpus is on the haystack most of the time. And one measured verdict flips: request `0b8b8382` (log line 28) was rejected on its rewrite for the single token `90` in a turn that called `get_dream_memory` (line 29); after this commit a bare `90` grounds off the seed's "−90% EV" (a `%` token feeds `quotedValues` too, `:541`). The reply text is not logged (replyHash only), so whether that 90 was an honest "90 hours old" or a fabrication is unknowable — which is the point.
`dataWarning` (the part the builder actually needed) is the lighter half: its numbers are `toFixed(1)` ages, and `g1` shows "**Over 8.5** is the play" grounding off an 8.5 h age (real total 11), "Chiefs-Ravens **over 45.5**" off a 45.5 h age, and "**94.2** innings" off a 94.2 h age. Alone this would be a `should` (it needs a 0.1 h coincidence); it is listed here because it is the same mechanism.
Fix (two parts, ~25 lines, keeps every new test green): (a) remove `dataWarning` from TEXT_VALUE_KEYS and add a reply-side exemption `collectAgeRaws` next to `collectTimeRaws` (`:788`) for `\d+(\.\d+)?\s*(?:h|hrs?|hours?)\b(?:\s+old)?` — the measured failure was the *reply's* "94 hours old", and exempting an age phrase adds nothing to the haystack (tests `:481-497` still pass: "94 hours old", "6h"). (b) Do not harvest `rule`/`reasoning`/`notes` into `quotedValues`/`quotedPercents`; if "9-2 on that side" must stay quotable (test `:571-574`), push memory-text numbers into `recordValues` only — that bucket grounds solely a claim the reply writes in `N-M` form (`:732-736`), so "9-2" passes and "20% edge" goes red. Then add the negative control in §Tests.

**2. [must] — `slate.ts:197-233` → a missing, malformed or stale odds snapshot is reported as a DARK league ("nothing on the board today, and no upcoming MLB game in the snapshot"), never as unreadable.**
`getOdds` never throws for those cases: `loadJsonWithStatus` returns the fallback `{events: []}` with `status: "missing"` (`tools/index.ts:81`) or the old data with `status: "stale"` (`:104`), and `getOdds` attaches a `dataWarning` string (`:368-372`). `buildTodaySlate` reads `res.fetchedAt`/`res.events` (`slate.ts:199-200`) and never `res.dataWarning`; the `catch` at `:201` covers only a thrown read (the only case the test at `slate.test.ts:213-226` exercises). Executed (`g2`, real module, injected odds shaped exactly as `getOdds` returns them):
- MLB file MISSING on a Saturday → MLB note `"MLB: nothing on the board today, and no upcoming MLB game in the snapshot."`, top note `"Nothing on the board anywhere today (Saturday, September 12, 2026 ET). Dark: MLB, NFL, NBA, WNBA."`; `JSON.stringify(payload).includes("dataWarning")` → **false**; `/missing|unreadable|not readable/` → **false**.
- MLB file 72 h STALE (all rows Wednesday's) → the same "nothing on the board today" note; `linesRefreshedEt` is populated (`"6:43 PM ET on September 9"`) but the note — the sentence the prompt says to read verbatim — asserts dark, and `nextSlateDateEt` is null, so "no upcoming MLB game in the snapshot" is printed on a game day.
Operability makes it worse: the loader's `MISSING`/`STALE` console warning is de-duplicated once per process (`tools/index.ts:62,111`), so after cold start there is no log line at all. The `/api/chat` trace does include `data/processed/**` (`next.config.ts:70`), so this fires on a failed refresh or a bad commit of `data/`, not on tracing — but a failed refresh is a weekly reality here (the model file was 94 h stale this session).
Fix: read `res.dataWarning`; when it is set, (a) carry it through on the league row (it is a TEXT_VALUE_KEY, so quoting it grounds), (b) when `games.length === 0` and the file is stale/missing/unknown, write the "not readable / last refreshed {linesRefreshedEt}, I can't see today's board" note instead of "nothing today", and (c) `console.warn` per turn with the league and status so `[chat/turn]` can be correlated. Add the negative control in §Tests.

**3. [should] — `router.ts:462-478` → `SCHEDULE_LEVEL_HINTS` capture bet-intent and non-calendar questions and send them to the no-pick calendar prompt, deterministically and ahead of the tiebreaker.**
Executed (`g3`, real `classifyDeterministic` with a September board). All of these route `B/MLB/schedule (schedule-level)`: "how did the board do last night?", "what's the desk's record on the board this week?", "who's on the mound tonight?", "who's on first?", "who's playing well lately?", "is anything on the board worth a unit tonight?", "any games today worth fading the public on?". The culprits are the bare `\b(?:today's|tonight's|the)\s+(?:slate|schedule|card|board)\b` alternative (`:472`) — "the board" is this product's word for its own picks — and `\bwho(?:'s| is| are)?\s+(?:playing|plays|on)\b` (`:470`). Slate-level bet intent wins only when it matches `SLATE_LEVEL_HINTS` first (`:646`), and "worth a unit"/"fading" do not. Live probe 1 ("is anything on the board worth a unit tonight?") landed on schedule scope; the model recovered by calling `get_board_edges` on its own and passed honestly, so the user-visible harm at n=1 was nil — but the prompt it ran under says "This is a CALENDAR answer, not a pick" (`persona.ts:108`), which is wrong for the question. Fix: drop the bare `the (slate|schedule|card|board)` and `who's on` alternatives; require the what/which/who + on/playing + today/tonight shape; add a negative list (`unit|bet|fade|edge|record|mound|pitch|prop`) so bet intent falls through to `looksSlateLevel`/the tiebreaker as before.

**4. [should] — `router.ts:464` + `slate.ts` → "tomorrow"/"Sunday" questions are tagged `schedule` but the tool only knows today, and the prompt forbids anything outside its payload (`persona.ts:128`), so the desk denies a board it is holding.**
Live probe 2, "what games are on tomorrow?" (2026-09-12, 9:23 PM ET): the reply opened with *"'Tomorrow' is Sunday, September 13, and that board isn't live yet"* and listed Saturday. Measured against the snapshots at that moment: the MLB file held **9** games dated 2026-09-13 ET and the NFL file **13**, all with prices. Honest about what it had, false about what exists. `g3` shows "what games are on tomorrow?", "which games are on Sunday?", "who's playing on Sunday night?", "what time do the games start on Sunday?" all route to schedule. Fix: either give `get_todays_slate` an optional `dayOffset` (0|1, still pure over `now`) and let the schedule prompt say "tomorrow" when asked, or exclude `tomorrow|sunday|monday|…|weekend` from the schedule hints so those fall through to the existing paths.

**5. [should] — `grounding.ts:133` + `:172` → `confidence` is now an already-percent key for EVERY producer, and one Lane B tool emits both scales in one payload.**
`get_trend_summary` (allow-listed at `laneB.ts:81`) returns a `SummaryLeague` (`tools/index.ts:753-765`). Measured in `data/processed/latest-summary.json`: `leagues[].confidence` = 1, 0.35, 1, 1 (0–1) and `bestBets[].confidence` = 61, 56, 53 (0–100). Before the commit the 61/56/53 were ×100'd and could not ground; now the 0.35 becomes 0.35 % and a truthful "35% confidence" cannot ground. Net effect is small and one-directional (a truthful answer gets a regen, never a fabrication passes). The right fix is at the producer (`free-stats-summary.ts:192` vs `:557/:859`) — one key, one scale — not in the guard.

**6. [nit] — `slate.ts:273` `gamesTodayByLeague` and `:41` `BETS_LANE_LEAGUES` are exported and used only by `slate.test.ts`;** `buildSlateEntities` computes its own `gamesToday` (`router.ts:271-276`). Dead surface with a test pinning it.

**7. [nit] — `fallback.ts:120-121` and `:54` → argument-error notes and de-hyphenated filenames reach the user.** `available:false` notes that are not data gaps ("player name is required", `stats.ts:332`; "team name is required", `:896`) become "What's missing: player name is required"; `stats.ts:919` embeds the model-chosen `team` string in its note, which is then reflected into the reply; unknown filenames render as "the mlb pitchers today snapshot". None leaks a path or a secret and the leak guard still runs on the result (`sharp.ts:614`).

## Kingsbury pass

1. **The guard is context-free, and every text corpus added to it is collision surface.** Interleaving: turn calls `get_dream_memory` (11/17 turns) → model hallucinates "an 18% edge" on tonight's game → `groundsAsQuoted` finds 18 in a parlay rule about longshots → `laneB_grounded` → a reader sizes a bet on a fabricated edge. The pre-existing `note`/`passReason` keys had the same property; this commit is the first time a large, number-dense, LLM-written corpus went into the bucket. (Finding 1.)
2. **A data outage has no invariant that stops it becoming a schedule.** "A league with a file refreshed three days ago is not dark" is enforced nowhere; the tool emits the same `gameCount: 0` for "no games", "file missing", "file is Wednesday's". The router's date-aware count (`router.ts:271-276`) has the same blind spot, so on a stale-file day the entity index also says every league is dark and `primaryLeagueWithGames` returns null — consistent, and consistently wrong. (Finding 2.)
3. **Two clocks, two file reads per turn.** `buildSlateEntities` (`router.ts:245`, `new Date()`) and `buildTodaySlate` (`slate.ts:189`) each read the snapshots and the clock independently; the persona line reads the clock a third time (`persona.ts:80`) and the reground a fourth. A turn straddling midnight ET, or an odds refresh landing between the reads, can route on one board and list another. At this traffic it is a curiosity; if `now` is ever threaded through `SharpDeps` (`sharp.ts:271` already has one), pass it to all four.

## Majors pass

- **What the log answers now:** `[chat/turn]` carries `toolsUsed`, `grounded`, and the new outcome names (`laneB_schedule_fallback`, `laneB_slate_fallback`, `sharp.ts:536-540`); the tiebreak substitution logs both the model's league and the substitute with the requestId (`:391-401`). Good.
- **What it cannot answer at 3 am:** *"why did the desk say nothing is on?"* — a league reporting 0 games because its file was stale/missing produces no per-turn line (the loader warns once per process, `tools/index.ts:62,111`), and the payload carries no warning (Finding 2). *"what did it actually say?"* — replyHash only; pre-existing and deliberate, but it is why the flipped `90` verdict above is unknowable.
- **Config/boot:** no new env or config. **Migration:** none. **Rollback:** revert the commit; no schema, no data files in `2ace92f1` (the `data/processed` modifications in the tree are uncommitted and not part of it).
- **Cost:** schedule turns skip the Haiku tiebreaker (deterministic route, `router.ts:666`). The date line sits inside the cached system block (`laneB.ts` `cachedSystem`) at day resolution in `America/New_York` (`persona.ts:68-72`); byte-stable across the UTC day boundary — proven by `schedule-routing.test.ts:313-317` and visible in the log (consecutive schedule turns at lines 94/96 report `cacheReadTokens: 14504`). One cache miss per ET day is the intended price.
- **Timezone:** `etDayKey`/`etClock` use `Intl` with an explicit zone; executed at 12:05 AM ET, 11:59 PM ET, both DST transitions and a December evening — correct day keys and clocks, no U+202F on this ICU (77.1), and `slate.ts:90` strips it anyway. `process.env.TZ` is unset locally and the output is still ET, so the Vercel UTC runtime is covered. Doubleheaders: two same-day rows both list with their own times and prices; tomorrow's row is excluded (`g2` case 3). In-progress: `started` is `start <= now` (`slate.ts:159`).

## Security pass

- **Guard order is unchanged and the schedule scope sits behind all of it:** distress (`sharp.ts:274`) → spend ceiling (`:288`) → router, where NFL → receipts (`router.ts:621`) and the refuse tier (`:632-641`) both precede the schedule branch (`:666`) → the injection gate wraps every Lane B resolution (`:339`) so an injection attempt never reaches schedule scope → route-level session/IP caps run before `answer()` (`route.ts:102-178`). "what NFL games are on today?" → lane `R` is pinned by `schedule-routing.test.ts:176-179`.
- **Inputs:** `get_todays_slate` takes no arguments (`laneB.ts:56-60`); no user text reaches `slate.ts`. The regex additions are anchored word patterns with no catastrophic backtracking shape.
- **Reflection:** `collectGaps` copies a tool's `note` verbatim into the reply (`fallback.ts:121`); one note embeds a model-supplied team string (`stats.ts:919`). Rendered by React; not an injection path; noted under Finding 7.
- **Leakage:** the fallback contains no `.json`, no "tool", no "loading" for the fixture shapes (`fallback.test.ts:162-176`) and `checkLeak` runs on every fallback (`sharp.ts:614`). Unknown filenames are de-hyphenated, not hidden (Finding 7). No secrets in the diff.
- **NFL on the default lane:** on a Sunday `get_todays_slate` will return NFL moneylines to a `/`-scoped schedule turn; the prompt says schedule-only (`persona.ts:116`) and the tool exposes no NFL edge or side, but the schedule scope still carries the full bets tool menu and `checkBettingClaims` is not applied — the same structural gap the pre-existing slate scope has. Not exercisable on 2026-09-12 (NFL dark); flagged as unverified rather than as a finding.
- **Agent isolation:** `grep -rn "slate\|get_todays" src/lib/agent/` → no reference; `grep -rn 'from "@/lib/chat' src/lib/agent/ src/lib/*.ts` → none. `TOOL_DEFINITIONS` (`tools/index.ts:1053`) is untouched; the chat-only tool is added at `laneB.ts:97/120` and the allowlist invariant test still enumerates the write/control names (`laneB-allowlist.test.ts`).

## Tests verdict

Ran green: 14 files / 360 tests. The new tests are real, not hollow: `slate.test.ts:148-193` asserts specific matchups, ET times, prices, `started` flags and the dark-league next dates; `fallback.test.ts:122-176` asserts the exact sentences and runs `checkLeak` on every shape; `schedule-routing.test.ts:235-285` drives the real `answer()` with a stubbed runner and asserts the league that reaches it. Negative controls that go red if the guard is weakened: `grounding.test.ts:499-509` (63 %, −155 vs the stale board), `:541-545` (80 %), `:576-579` (14-1), `:632-639` (−190).

What the suite does not test is the threat the loosening introduced, because a context-free guard cannot fail it by construction. **The one missing test that catches Finding 1:** `checkGrounding("Braves are a 20% edge tonight.", [oddsPayload, JSON.stringify({ rules: PARLAY_LEARNINGS.map(...) })])` → `grounded === false`. It is red today (`g1`). **For Finding 2:** `buildTodaySlate({ now, odds: () => ({ fetchedAt: null, events: [], dataWarning: "DATA ERROR: … is missing" }) })` → the MLB note must not contain "nothing on the board today". Red today (`g2`).

---

## Summary

1. Verdict: **ANOTHER ROUND** — two musts, three shoulds, two nits; refuted at the gate: 0 (every must was proven by execution).
2. The commit does fix the three measured failures (staleness "94" rejections, October-NBA misroute, matchup-shaped fallback); 360 tests green, tsc clean for `src/lib/chat`, timezone handling correct at every boundary tried.
3. **Must 1** — `grounding.ts:427-434,539-541`: `rule`/`reasoning`/`notes`/`dataWarning` numbers now ground any claim; with only the four committed memory seeds, fabricated "20% edge", "90%", "18%", "15.1%", "2.4%" all pass (all rejected before); `get_dream_memory` is on 11 of 17 logged turns.
4. **Must 2** — `slate.ts:197-233`: a missing or stale odds file is reported as "nothing on the board today, and no upcoming MLB game" with no warning in the payload and no per-turn log line.
5. **Should** — `router.ts:462-478` schedule regexes capture "the board"/"who's on" bet questions; "tomorrow"/"Sunday" go to a today-only tool (probe 2 denied a board holding 9 MLB + 13 NFL Sunday rows); `confidence` is one key with two scales in `get_trend_summary`.
6. Guard order intact: distress → spend → NFL receipts / refusal → injection gate → schedule; `get_todays_slate` is chat-only (no reference under `src/lib/agent/`) and argument-free.
7. Fixes are small: an age-phrase reply exemption instead of harvesting `dataWarning`; memory text into the record-only bucket; carry `dataWarning` through the slate row and never write "nothing today" for a stale/missing file; tighten two regex alternatives.
8. Round 2 needs: the two red tests above committed and green, plus a re-run of `npx vitest run src/lib/chat`; no live probes required.
